# Python backend
med_work_backend/__pycache__/
med_work_backend/**/*.py[cod]
med_work_backend/*.egg-info/
med_work_backend/.venv/
med_work_backend/venv/
med_work_backend/.env
med_work_backend/dist/
med_work_backend/build/
med_work_backend/.idea/
med_work_backend/.vscode/
med_work_backend/.pytest_cache/
med_work_backend/.mypy_cache/

# Frontend dependencies and generated files
med_work_frontend/node_modules/
med_work_frontend/dist/
med_work_frontend/*.tsbuildinfo
med_work_frontend/*.d.ts

# med_work_admin（Vben Admin monorepo，pnpm workspace）
# 依赖：根与各子包（internal/*、packages/*）都会生成 node_modules
med_work_admin/node_modules/
med_work_admin/**/node_modules/
# 构建产物：应用产物 + 各子包 tsdown 打包输出（含 packages/@core/base/shared/dist）
med_work_admin/dist/
med_work_admin/**/dist/
med_work_admin/*.tsbuildinfo
med_work_admin/**/*.tsbuildinfo
med_work_admin/*.d.ts
# 本地手工打包上传的 zip（如 dist.zip）
med_work_admin/*.zip
med_work_admin/**/*.zip
# 构建/测试缓存
med_work_admin/.turbo/
med_work_admin/**/.turbo/
med_work_admin/**/.vite/
med_work_admin/**/vite.config.ts.timestamp-*
med_work_admin/**/vite.config.*.timestamp-*
med_work_admin/coverage/
med_work_admin/**/coverage/
med_work_admin/docs/.vitepress/cache/
med_work_admin/docs/.vitepress/dist/
med_work_admin/.eslintcache
med_work_admin/.stylelintcache

# Legacy duplicate generated during repository migration
med_work_backend/frontend/

# Workspace-only tooling
.venv/
.agents/
.playwright-cli/
skills-lock.json

# 服务运行日志（运行时生成，不应提交）
**/_uvicorn*.log
**/_vite.log
**/logs/*.log

# 前端环境文件（保留 .env.example / .env.development 等共享模板，忽略含敏感信息的本地文件）
med_work_frontend/.env
med_work_frontend/.env.local
med_work_frontend/.env.*.local
med_work_admin/.env
med_work_admin/.env.local
med_work_admin/.env.*.local

# RAG 服务：.env 含真实 API 密钥与数据库密码，仅提交 .env.example
med_rag_service/.env

# RAG 服务：本地下载的向量模型（约 391MB），由 scripts/ 重新下载
med_rag_service/models/

# RAG 服务：运行时数据（Qdrant 向量库、用户上传文件）
med_rag_service/data/qdrant/
med_rag_service/data/uploads/

# 运行时日志（补充上方的 logs/*.log 规则）
med_work_frontend/_vite*.log
med_work_admin/_vite*.log
med_work_admin/*.log
med_work_admin/**/*.log

# IDE / 本地工具产物
.idea/
.codex/
.codebuddy/
.vscode/

# 操作系统垃圾文件
.DS_Store
Thumbs.db

# 服务器部署打包产物（本地手工上传用，不提交）
/*.zip
