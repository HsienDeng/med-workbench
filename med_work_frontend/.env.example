# ===== 前端环境变量模板（Vite 读取，仅构建/开发期生效）=====

# 本地开发代理目标：npm run dev 时，页面 /api 请求会转发到该地址
# 默认 http://localhost:8001（不配置也有兜底，见 vite.config.ts）
VITE_API_TARGET=http://localhost:8001

# ---------------------------------------------------------------
# 用法说明
# ---------------------------------------------------------------
# 1) 复制本文件为 .env.development 即可用于本地开发：
#      Copy-Item .env.example .env.development   (Windows)
#      cp .env.example .env.development          (Linux/macOS)
# 2) 变量仅在 .env.development / .env.production 等模式文件中按需覆盖，
#    只有 VITE_ 前缀的变量会暴露给前端代码（import.meta.env.VITE_*）。
# 3) 生产环境无需任何前端 .env 配置：
#    构建产物请求为相对路径 /api/*，由 Nginx 同源反向代理到后端 8001。
# ---------------------------------------------------------------
